# =====================================================
# SecondBrain - Variables de entorno
# Copiar a .env y completar
# =====================================================

# -----------------------------------------------------
# PostgreSQL
# -----------------------------------------------------
POSTGRES_USER=secondbrain
POSTGRES_PASSWORD=cambiar_password_seguro_aqui
POSTGRES_DB=secondbrain

# -----------------------------------------------------
# Qdrant
# -----------------------------------------------------
QDRANT_API_KEY=cambiar_key_seguro_aqui

# -----------------------------------------------------
# Ollama
# -----------------------------------------------------
# qwen3:8b (chat + tagger) y qwen3-vl:8b (visión) ganaron el benchmark inicial.
# El embedding se migró de qwen3-embedding:4b a bge-m3 tras el A/B del 2026-05-16
# (backend/ab_embedding.py): mejor recall en español rioplatense y ~1.2GB en VRAM
# contra ~3.8GB del anterior — entra junto con qwen3:8b sin hacer swap en 8GB.
# Ver docs/pipeline.md §10.1-10.2 para el detalle y los números medidos.
OLLAMA_MODEL_PRIMARY=qwen3:8b
OLLAMA_MODEL_VISION=qwen3-vl:8b
OLLAMA_MODEL_EMBEDDING=bge-m3

# -----------------------------------------------------
# Whisper
# -----------------------------------------------------
WHISPER_MODEL=large-v3-turbo
WHISPER_LANG=es

# -----------------------------------------------------
# Backend
# -----------------------------------------------------
BACKEND_PORT=8000
BACKEND_LOG_LEVEL=info
BACKEND_SECRET_KEY=cambiar_secret_key_aqui_minimo_32_caracteres

# -----------------------------------------------------
# Frontend (Streamlit)
# -----------------------------------------------------
FRONTEND_PORT=8501

# -----------------------------------------------------
# Bridge WhatsApp (Baileys) — Sprint 2
# -----------------------------------------------------
BRIDGE_PORT=3001
BRIDGE_LOG_LEVEL=warn
# Capturar también los mensajes que envío yo (true/false)
BRIDGE_CAPTURE_OUTGOING=true
# Capturar listas de difusión (@broadcast). Los Estados de WhatsApp se filtran siempre.
BRIDGE_INCLUDE_BROADCASTS=false

# -----------------------------------------------------
# Timezone
# -----------------------------------------------------
TZ=America/Argentina/Buenos_Aires
